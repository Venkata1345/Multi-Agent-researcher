# Observability

_What to look at in LangSmith. Filled out in Phase 2/4._

Planned: every agent run, tool call, and LLM call traced as a nested tree;
per-agent latency, token count, and estimated cost attached as run metadata.
